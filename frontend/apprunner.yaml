version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - npm ci
      - npm run build
      - npm install -g serve
run:
  runtime-version: 18
  command: serve -s build -l 3000
  network:
    port: 3000
    env: PORT
  env:
    - name: PORT
      value: "3000"